# Build output
build/
*.class
__pycache__/

# The engine is a pinned upstream clone, not our source. setup.sh restores it
# at the exact commit this project was verified against.
cubiomes/

# Generated by ask.py / test runs
queries/_*.json

# --- GPL-3.0 quarantine -------------------------------------------------
# These are cubiomes-viewer source files (Qt; CAT_QUAD/CAT_STRUCT condition
# system) that landed in the project root during research. cubiomes-viewer is
# GPL-3.0; this project links MIT cubiomes only and stays MIT-compatible.
#
# They are inert -- not referenced by build.sh, src/, or tools/ -- and none of
# our code derives from them (verified: zero shared identifiers). They are
# excluded from version control so the repo stays license-clean. Safe to
# delete; kept only because they weren't ours to remove.
search.h
search.cpp
searchthread.cpp
cub_readme.md
